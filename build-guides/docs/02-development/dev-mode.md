# 开发模式

本文档介绍如何在开发环境中启动 Huobao Drama 项目的前后端服务，以及如何进行开发调试。

## 后端开发模式

### 启动后端服务

后端服务基于 Go + Gin 框架，支持开发模式下的快速启动和调试。

#### 进入后端目录

```bash
cd huobao-drama
```

#### 启动开发服务器

使用 `go run` 命令直接运行主程序：

```bash
go run main.go
```

这将启动后端 API 服务器，并在控制台输出启动日志。

### 后端服务信息

- **监听端口**: 默认 `8080`
- **访问地址**: `http://localhost:8080`
- **API 基础路径**: `http://localhost:8080/api`

### 验证后端服务

启动成功后，你可以通过以下方式验证服务是否正常运行：

```bash
# 使用 curl 测试 API
curl http://localhost:8080/api/health

# 或在浏览器中访问
# http://localhost:8080/api/health
```

### 查看后端日志

后端服务会在控制台输出详细的日志信息，包括：

- **启动信息**: 服务器启动状态、监听端口
- **请求日志**: HTTP 请求方法、路径、响应状态码、耗时
- **错误日志**: 运行时错误、异常堆栈信息
- **业务日志**: AI 服务调用、视频处理进度等

**日志级别说明**:
- `DEBUG`: 详细的调试信息
- `INFO`: 一般信息性消息
- `WARN`: 警告信息，不影响正常运行
- `ERROR`: 错误信息，需要关注

ℹ️ **提示**: 开发模式下默认启用 DEBUG 级别日志，可以看到详细的执行信息。


## 前端开发模式

### 启动前端服务

前端应用基于 Vue3 + TypeScript + Vite，支持热重载（Hot Module Replacement）功能。

#### 进入前端目录

```bash
cd huobao-drama/web
```

#### 启动开发服务器

使用 npm 或 pnpm 启动开发服务器：

**使用 npm**:
```bash
npm run dev
```

**使用 pnpm** (推荐):
```bash
pnpm dev
```

💡 **最佳实践**: pnpm 安装速度更快，占用磁盘空间更小，推荐在开发环境中使用。

### 前端服务信息

- **开发服务器端口**: 默认 `5173`
- **访问地址**: `http://localhost:5173`
- **热重载**: 已启用，代码变更自动刷新

### 访问前端应用

启动成功后，在浏览器中访问：

```
http://localhost:5173
```

你将看到 Huobao Drama 的主界面。

### 热重载功能

前端开发服务器启用了 Vite 的热模块替换（HMR）功能：

- **自动刷新**: 修改 `.vue`、`.ts`、`.css` 等文件后，浏览器会自动刷新
- **状态保持**: 大多数情况下，组件状态会在刷新后保持
- **即时反馈**: 代码变更通常在 1 秒内反映到浏览器

ℹ️ **提示**: 如果热重载未生效，尝试手动刷新浏览器（F5）或重启开发服务器。

### 配置代理连接后端 API

前端开发服务器已配置代理，自动将 API 请求转发到后端服务。

#### 代理配置说明

在 `vite.config.ts` 中已配置：

```typescript
export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  }
})
```

这意味着：
- 前端发送到 `/api/*` 的请求会自动转发到 `http://localhost:8080/api/*`
- 无需在前端代码中硬编码后端地址
- 避免跨域（CORS）问题

#### 修改后端地址

如果后端服务运行在不同的端口或主机，修改 `vite.config.ts` 中的 `target` 配置：

```typescript
proxy: {
  '/api': {
    target: 'http://localhost:9000',  // 修改为实际的后端地址
    changeOrigin: true
  }
}
```

⚠️ **注意**: 修改配置文件后需要重启前端开发服务器才能生效。

### 前端开发工具

#### Vue DevTools

推荐安装 Vue DevTools 浏览器扩展，用于调试 Vue 组件：

- **Chrome**: [Vue.js devtools](https://chrome.google.com/webstore/detail/vuejs-devtools/nhdogjmejiglipccpnnnanhbledajbpd)
- **Firefox**: [Vue.js devtools](https://addons.mozilla.org/en-US/firefox/addon/vue-js-devtools/)

#### 浏览器开发者工具

使用浏览器的开发者工具（F12）可以：
- 查看网络请求和响应
- 调试 JavaScript 代码
- 检查 DOM 结构和样式
- 查看控制台日志和错误


## 并行启动前后端

在开发过程中，通常需要同时运行前端和后端服务。以下是不同平台的并行启动方法。

### 使用多个终端窗口

最简单的方法是使用多个终端窗口或标签页：

#### Windows Terminal (推荐)

Windows Terminal 支持多标签页和分屏功能：

1. **打开第一个标签页**，启动后端：
   ```bash
   cd huobao-drama
   go run main.go
   ```

2. **新建标签页** (Ctrl + Shift + T)，启动前端：
   ```bash
   cd huobao-drama/web
   pnpm dev
   ```

3. **分屏显示** (可选):
   - 按 `Alt + Shift + D` 垂直分屏
   - 按 `Alt + Shift + -` 水平分屏
   - 可以同时查看前后端日志

💡 **提示**: 在 Windows Terminal 设置中可以自定义快捷键和外观。

#### PowerShell / CMD

如果使用传统的 PowerShell 或 CMD：

1. 打开第一个窗口，启动后端
2. 打开第二个窗口，启动前端
3. 使用 `Alt + Tab` 在窗口间切换

### Linux / macOS 终端方案

#### 使用 tmux (推荐)

tmux 是一个强大的终端复用工具，可以在一个窗口中管理多个会话：

**安装 tmux**:
```bash
# Ubuntu/Debian
sudo apt install tmux

# macOS
brew install tmux
```

**使用 tmux 启动服务**:

```bash
# 创建新会话
tmux new -s huobao

# 启动后端
cd huobao-drama
go run main.go

# 分割窗口 (Ctrl+B 然后按 ")
# 在新窗格中启动前端
cd huobao-drama/web
pnpm dev

# 切换窗格: Ctrl+B 然后按方向键
# 退出 tmux: Ctrl+B 然后按 D (detach)
# 重新连接: tmux attach -t huobao
```

**常用 tmux 快捷键**:
- `Ctrl+B "`: 水平分割窗格
- `Ctrl+B %`: 垂直分割窗格
- `Ctrl+B 方向键`: 切换窗格
- `Ctrl+B D`: 分离会话（后台运行）
- `Ctrl+B X`: 关闭当前窗格

#### 使用 screen

screen 是另一个终端复用工具，功能类似 tmux：

```bash
# 创建新会话
screen -S huobao

# 启动后端
cd huobao-drama
go run main.go

# 创建新窗口 (Ctrl+A 然后按 C)
# 启动前端
cd huobao-drama/web
pnpm dev

# 切换窗口: Ctrl+A 然后按 N (下一个) 或 P (上一个)
# 分离会话: Ctrl+A 然后按 D
# 重新连接: screen -r huobao
```

#### 使用后台进程

也可以将服务作为后台进程运行：

```bash
# 启动后端（后台运行）
cd huobao-drama
nohup go run main.go > backend.log 2>&1 &

# 启动前端（后台运行）
cd web
nohup pnpm dev > frontend.log 2>&1 &

# 查看日志
tail -f ../backend.log
tail -f frontend.log

# 停止服务
pkill -f "go run main.go"
pkill -f "pnpm dev"
```

⚠️ **注意**: 后台运行方式不便于实时查看日志和调试，建议仅在特殊情况下使用。

## 日志查看和过滤

### 实时查看日志

开发模式下，日志会直接输出到控制台。如果需要保存日志到文件：

**后端日志重定向**:
```bash
go run main.go 2>&1 | tee backend.log
```

**前端日志重定向**:
```bash
pnpm dev 2>&1 | tee frontend.log
```

这样可以同时在控制台查看日志，并保存到文件中。

### 日志过滤

使用 `grep` 过滤特定日志：

```bash
# 只查看错误日志
go run main.go 2>&1 | grep ERROR

# 只查看 API 请求日志
go run main.go 2>&1 | grep "GET\|POST\|PUT\|DELETE"

# 排除调试日志
go run main.go 2>&1 | grep -v DEBUG
```

在 Windows PowerShell 中使用 `Select-String`：

```powershell
# 只查看错误日志
go run main.go 2>&1 | Select-String "ERROR"

# 只查看 API 请求日志
go run main.go 2>&1 | Select-String "GET|POST|PUT|DELETE"
```

## 开发调试技巧

### 后端调试

#### 使用 Delve 调试器

Delve 是 Go 语言的专用调试器：

```bash
# 安装 Delve
go install github.com/go-delve/delve/cmd/dlv@latest

# 使用 Delve 启动应用
dlv debug main.go

# 在 Delve 中设置断点
(dlv) break main.main
(dlv) continue
```

#### 使用 VS Code 调试

在 VS Code 中配置 `.vscode/launch.json`：

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Backend",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/huobao-drama/main.go"
    }
  ]
}
```

然后按 F5 启动调试。

#### 添加调试日志

在代码中添加详细的日志输出：

```go
import "github.com/sirupsen/logrus"

logrus.WithFields(logrus.Fields{
    "user_id": userID,
    "action": "create_drama",
}).Debug("Processing request")
```

### 前端调试

#### 使用浏览器 DevTools

1. 打开浏览器开发者工具（F12）
2. 在 Sources 标签页中设置断点
3. 刷新页面，代码会在断点处暂停

#### 使用 console.log

在代码中添加调试输出：

```typescript
console.log('API Response:', response.data)
console.table(items) // 以表格形式显示数组
console.dir(object)  // 显示对象的详细信息
```

#### 使用 Vue DevTools

Vue DevTools 可以：
- 查看组件树和组件状态
- 检查 Vuex/Pinia store 状态
- 追踪事件和路由变化
- 性能分析

### 常见开发问题

#### 端口被占用

如果端口已被占用，可以：

**查找占用端口的进程**:

Windows:
```cmd
netstat -ano | findstr :8080
taskkill /PID <进程ID> /F
```

Linux/macOS:
```bash
lsof -i :8080
kill -9 <进程ID>
```

**或修改配置使用其他端口**:
- 后端: 修改 `config.yaml` 中的 `server.port`
- 前端: 修改 `vite.config.ts` 中的 `server.port`

#### 热重载不工作

如果前端热重载失败：

1. 检查文件监听限制（Linux）:
   ```bash
   # 增加文件监听数量
   echo fs.inotify.max_user_watches=524288 | sudo tee -a /etc/sysctl.conf
   sudo sysctl -p
   ```

2. 重启开发服务器
3. 清除浏览器缓存（Ctrl + Shift + R）

#### API 请求失败

如果前端无法连接后端：

1. 确认后端服务已启动并监听正确端口
2. 检查 `vite.config.ts` 中的代理配置
3. 查看浏览器控制台的网络请求错误
4. 检查后端 CORS 配置

#### 依赖版本冲突

如果遇到依赖问题：

```bash
# 清除 Go 模块缓存
go clean -modcache
go mod download

# 清除前端依赖
rm -rf node_modules
rm pnpm-lock.yaml
pnpm install
```

---

💡 **开发流程建议**:

1. **启动顺序**: 先启动后端，再启动前端
2. **日志监控**: 保持终端可见，实时查看日志
3. **定期重启**: 修改配置文件后需要重启服务
4. **版本控制**: 频繁提交代码，避免丢失工作
5. **测试验证**: 修改后及时测试功能是否正常

